$('.custombox-content').remove();
$('.custombox-overlay').remove();
$('.custombox-blur').removeClass('custombox-blur').removeClass('custombox-container').removeClass('custombox-top').removeClass('custombox-open');

$('.message').html('Message sent');
$('.message').prop('disabled', true);
